struct Token {
  type: string,
  lexeme: string,
  line: i32
}

fn test(): void {
  var i: i32 = 0

  while i < 3 {
    var x: string = "test"
    println(x)
    i = i + 1
  }
}

fn main(): void {
  test()
}
